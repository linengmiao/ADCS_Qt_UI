This code is the graphical user interface of the ADCS-project. The calculated values of the rotation 
matrices and quaternions that identify the orientation of the spacecraft are sent via UART to this 
application which runs on the computer itself. 
The data is parsed and rendered via: 3D model in openGL, graphs, tables,...